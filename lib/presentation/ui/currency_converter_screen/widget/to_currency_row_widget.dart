import 'package:flutter/material.dart';

class ToCurrencyRowWidget extends StatelessWidget {
  const ToCurrencyRowWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
